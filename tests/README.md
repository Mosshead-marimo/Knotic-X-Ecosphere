# Cross-service tests

Tests spanning component boundaries are organized by purpose:

- `contract`: API, event, data, and MCP compatibility.
- `e2e`: complete customer and operator journeys.
- `performance`: latency, load, capacity, and soak tests.
- `security`: authentication, isolation, injection, secret, and abuse tests.

Component-local tests stay beside their owning component.

