# Infrastructure

Infrastructure definitions are grouped by concern:

- `docker`: local and production container definitions.
- `deployment`: environment and release topology.
- `observability`: logging, metrics, tracing, dashboards, and alerts.

This directory currently defines ownership only. Service topology is implemented by `P0-T008` after runtimes, dependencies, and configuration contracts are established.

