# Shared configuration package

Pure typed primitives for environment selection, explicit secret-provider resolution, safe configuration errors, and logging redaction. It contains no service-specific credentials or I/O beyond the default environment secret provider.

Backend and MCP own their required fields and validation in their respective packages.

